default 0
timeout 5
showmenu 1

title Debian 6.1 + Loongson VGA console (test, with initrd)
    kernel (usb0,0)/vmlinux
    initrd (usb0,0)/initrd.gz
    args console=ttyS0,115200 console=tty loglevel=7 rdinit=/init

title Debian 6.1 + Loongson VGA console (kernel only, no initrd)
    kernel (usb0,0)/vmlinux
    args console=ttyS0,115200 console=tty loglevel=7
